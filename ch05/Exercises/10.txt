The switch statment evalutes 1 % 3 (the remainder of 1 / 3) and prints the result
in string format. In this case 1 % 3 = 1, so case 0: is executed to print "one".
But due to the lack of `break`, it will print one more `two`.

So, the answer is `onetwo`.
